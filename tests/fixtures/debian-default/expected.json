{
  "description": "Stock Debian, privileged (opted in) but with no `sshd -T` grant, so the ssh checks fall back to the file. Default root login (prohibit-password), password auth on, MaxAuthTries 6, idle sessions not timed out (ClientAliveInterval unset), no password expiry, umask 022, a duplicate UID (admin shares 1000 with debian), several insecure sysctls (accept_redirects, dmesg_restrict, kptr_restrict, unprivileged_bpf), no host firewall, pending security updates, auditd absent.",
  "privileged": true,
  "findings": {
    "ssh-permit-root-login": "fail",
    "ssh-password-authentication": "fail",
    "ssh-permit-empty-passwords": "pass",
    "ssh-x11-forwarding": "pass",
    "ssh-max-auth-tries": "fail",
    "ssh-login-grace-time": "fail",
    "ssh-client-alive-interval": "fail",
    "ssh-permit-tunnel": "pass",
    "ssh-weak-crypto": "pass",
    "accounts-nonroot-uid0": "pass",
    "accounts-pass-max-days": "fail",
    "accounts-umask": "fail",
    "accounts-duplicate-uid": "fail",
    "accounts-system-login-shells": "pass",
    "accounts-shadow-empty-password": "pass",
    "accounts-shadow-weak-hash": "pass",
    "kernel-aslr": "pass",
    "kernel-tcp-syncookies": "pass",
    "kernel-rp-filter": "pass",
    "kernel-ip-forward": "pass",
    "kernel-accept-redirects": "fail",
    "kernel-source-route": "pass",
    "kernel-ptrace-scope": "pass",
    "kernel-dmesg-restrict": "fail",
    "kernel-kptr-restrict": "fail",
    "kernel-suid-dumpable": "pass",
    "kernel-unprivileged-bpf": "fail",
    "firewall-enabled": "fail",
    "firewall-nft-default-deny": "skipped",
    "updates-security-pending": "fail",
    "updates-auto-updates": "fail",
    "services-cleartext-ports": "pass",
    "services-rpcbind": "pass",
    "services-fail2ban": "fail",
    "logging-auditd": "fail",
    "logging-syslog": "pass"
  },
  "scores": {
    "baseline": 45,
    "hardened": 30
  }
}
