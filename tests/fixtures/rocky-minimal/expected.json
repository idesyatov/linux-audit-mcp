{
  "description": "Rocky/RHEL host, privileged (opted in): root login and password auth on, X11 forwarding on, no password expiry, ip_forward enabled, ptrace unrestricted, suid_dumpable on, unprivileged BPF on, telnet listening, rpcbind enabled. firewalld/auditd/rsyslog on. apt-get is absent (updates check errors). The file sshd_config lists weak ciphers/MACs, but `sshd -T` shows the effective set (crypto-policy hardened) has none, so ssh-weak-crypto passes on the effective config.",
  "privileged": true,
  "findings": {
    "ssh-permit-root-login": "fail",
    "ssh-password-authentication": "fail",
    "ssh-permit-empty-passwords": "pass",
    "ssh-x11-forwarding": "fail",
    "ssh-max-auth-tries": "pass",
    "ssh-login-grace-time": "fail",
    "ssh-weak-crypto": "pass",
    "accounts-nonroot-uid0": "pass",
    "accounts-pass-max-days": "fail",
    "accounts-umask": "pass",
    "accounts-shadow-empty-password": "fail",
    "kernel-aslr": "pass",
    "kernel-tcp-syncookies": "pass",
    "kernel-rp-filter": "pass",
    "kernel-ip-forward": "fail",
    "kernel-accept-redirects": "pass",
    "kernel-source-route": "pass",
    "kernel-ptrace-scope": "fail",
    "kernel-dmesg-restrict": "pass",
    "kernel-kptr-restrict": "pass",
    "kernel-suid-dumpable": "fail",
    "kernel-unprivileged-bpf": "fail",
    "firewall-enabled": "pass",
    "firewall-nft-default-deny": "pass",
    "updates-security-pending": "error",
    "updates-auto-updates": "pass",
    "services-cleartext-ports": "fail",
    "services-rpcbind": "fail",
    "services-fail2ban": "fail",
    "logging-auditd": "pass",
    "logging-syslog": "pass"
  },
  "scores": {
    "baseline": 31,
    "hardened": 7
  }
}
